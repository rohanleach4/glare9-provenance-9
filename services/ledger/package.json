{
  "name": "@glare9/provenance-ledger-service",
  "version": "0.1.0-alpha.2",
  "description": "Authenticated ingestion service for Provenance•9",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0 <25"
  },
  "dependencies": {
    "@glare9/provenance": "file:../..",
    "@glare9/provenance-connector-contract": "file:../../packages/connector-contract"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/main.js",
    "test": "node --test test/*.test.js"
  }
}
